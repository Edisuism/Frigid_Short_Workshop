using System;
using UnityEngine;


public class IceStructureBreak : MonoBehaviour
{
    public GameObject mistEffect;
    
    private void Start()
    {
        
    }

    public void Break()
    {
        // foreach (Vector3 pos in modifer.IcePositions)
        // {
        //     GameObject mistObject = Instantiate(mistEffect, pos, Quaternion.identity);
        //     mistObject.GetComponent<ParticleSystem>().Play();
        // }
        // Destroy(gameObject, destroyDelay);
    }
}